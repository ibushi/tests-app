document.addEventListener("DOMContentLoaded", (event) => {
    // Your code to run since DOM is loaded and ready

    var image = document.getElementById('image');
    image.style.border = '2px dotted black';
    console.log('Border line changed.')
});